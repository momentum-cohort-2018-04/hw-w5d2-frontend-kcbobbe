{
  "private": true,
  "devDependencies": {
    "babel-preset-env": "^1.7.0",
    "parcel-bundler": "^1.8.1"
  },
  "scripts": {
    "start": "parcel index.html",
    "build": "parcel build --no-source-maps index.html",
    "deploy": "npm run build && surge --domain katie-w5d2.surge.sh dist/",
    "test": "jest"
  },
  "dependencies": {
    "babel-jest": "^22.4.4",
    "jest": "^22.4.4",
    "superagent": "^3.8.3"
  }
}
